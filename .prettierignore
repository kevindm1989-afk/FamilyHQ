dist
dev-dist
coverage
node_modules
package-lock.json
design-tokens.json
*.md

# Generated CSS-variable theme layer — emitted verbatim by
# scripts/gen-theme-css.cjs from design-tokens.json. Its formatting is the
# generator's output, gated by the theme-css-drift test (not prettier); do not
# reformat or the drift check breaks.
src/index.theme.css

# Functions workspace build output (tsc emits to functions/lib/; gitignored
# but a freshly-built local tree leaves the files around for prettier to
# pick up). The source under functions/src/ IS prettier-checked.
functions/lib/
functions/node_modules/

# Design handoff artifacts (authored by the designer; not app source — do not reformat).
design/

# Pre-existing pack workflow files not authored in Phase 0 scaffolding.
.github/workflows/deploy.yml
.github/workflows/security-scan.yml

# Test specs + the rules harness are authored by the test-writer and are
# read-only to the implementer (committed already non-conformant). They are not
# shipped app source; do not reformat them here.
test/rules/
src/**/*.test.ts
src/**/*.test.tsx
src/features/auth/authService.test.ts
